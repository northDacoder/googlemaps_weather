if (Meteor.isClient) {
    









    
}



    /*function initLocationControls() {
        var geoMessage;
        if (Modernizr.geolocation) {
           $("#display-location-data").setAttr("disabled", false); 
           geoMessage = 'Use the button below to display location data';

           $("#display-location-data").click(function() {
              displayLocaitonData(); 
           });
        } else {
            geoMessage = 'Sorry, the geolocation API is not available';
            $("#display-location-data").setAttr("disabled", "disabled");
        }
        $("#status-message").val(geoMessage);
    }*/



